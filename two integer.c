#include<stdio.h>
int main()
{
    int first,second;
    printf("Please enter the first value:");
    scanf("%d", &first);

    printf("Now, enter the second value:");
    scanf("%d", &second);

    printf("The two integers are %d and %d", first,second);
    return 0;
}
