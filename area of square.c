#include<stdio.h>
int main()
{
    int side;
    printf("Please enter the side of square in cm: ");
    scanf("%d", &side);
    printf("The area of your square is %d cm*cm", side*side );
    return 0;
}
