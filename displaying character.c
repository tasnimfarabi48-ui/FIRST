#include<stdio.h>
int main()
{
    char first;
    printf("Please enter the character:");
    scanf("%c", &first);

    printf("The displayed character will be : %c", first);
    return 0;
}
